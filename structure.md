representation of structure that takes in both the existing backbone and the choice

using the osprey energy function

rotamer library

game space (how do we implement attaching a rotamer to a backbone)
